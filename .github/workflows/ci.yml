name: Genesis CI

on:
  push:
    branches: [main]

jobs:
  hello-runner:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Verify tools
        shell: bash
        run: |
          echo "Hello from self-hosted runner!"
          which docker
          which kubectl
          which helm
          docker --version
          kubectl version --client
          helm version
